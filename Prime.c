#include<stdio.h>
#include<conio.h>
void main()
{
    int num,i;
    printf("Enter Number");
    scanf("%d",&num);
    for(i=2; i<(num-1); i++)
    if (num)
    {
        /* code */
    }
    getch();
}